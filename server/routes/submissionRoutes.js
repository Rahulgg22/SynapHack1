// Submission routes
const express = require('express');
const router = express.Router();
const submissionController = require('../controllers/submissionController');

// Define submission routes here

module.exports = router;
